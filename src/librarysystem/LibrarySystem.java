package librarysystem;

import java.sql.Timestamp;
import java.util.Date;

public class LibrarySystem {

   // public static void main(String[] args) {
        //LibraryActions ac = new LibraryActions();
        //Librarian l = new Librarian();
        //User user = l.getUser(2);
        //user.updateDetails("Samir", "9101", "Home3");
        //User.register("Moataz", "1234", "Home");
        //LibraryItem item = new Book("978-0-13-468599-1", "Pearson", "Robert Sedgewick", 2019, 784, 4, "Algorithms", "Computer Science", Status.AVAILABLE);
        //Date dueDate = generateRandomDate();
        //l.addLibraryItem(item);
        //item.setAuthor("Hobo");
        //l.updateLibraryItem(item);
        //l.removeLibraryItem(item);
        //ac.lendItem(1, 1, dueDate);
        //ac.returnItem(1);
        //ac.searchLibraryItems("222");
        //l.displayLibraryItems();
        //l.displayUsers();
        //l.manageUser(, "update");
        //l.getUser(1);
        //l.trackBorrowings();
    }
    // Method to generate a random date
   // private static Date generateRandomDate() {
   //     long offset = Timestamp.valueOf("2022-01-01 00:00:00").getTime();
   //     long end = Timestamp.valueOf("2025-01-01 00:00:00").getTime();
   //     long diff = end - offset + 1;
   //     return new Date(offset + (long)(Math.random() * diff));
   // }
//}